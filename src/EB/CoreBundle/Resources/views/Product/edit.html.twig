{% extends '::base.html.twig' %}

{% form_theme edit_form 'EBCoreBundle:Form:fields.html.twig' %}

{% block content -%}
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <h1>Product edit</h1>
            {{ form(edit_form) }}
            <ul class="record_actions">
                <li>
                    <a href="{{ path('product') }}">
                        Back to the list
                    </a>
                </li>
                <li>{{ form(delete_form) }}</li>
            </ul>
        </div>
    </div>
{% endblock %}
